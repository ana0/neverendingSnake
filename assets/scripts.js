
const initBoard = () => {
  const width = window.innerWidth;
  const height = window.innerHeight;
  const board = new Array(height/10)
}

const initGame = () => {

}

const setup = () => {
}

const draw = () => {
  // box();
  ellipse(50, 50, 10, 10);
}